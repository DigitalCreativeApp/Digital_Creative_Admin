export function LoadingState({ variant = 'content' }: { variant?: 'content' | 'dashboard' }) { return <div className={`skeleton-layout ${variant}`} role="status" aria-label="Đang tải dữ liệu"><div className="skeleton skeleton-heading"/><div className="skeleton-grid">{Array.from({ length: variant === 'dashboard' ? 4 : 6 }).map((_, index) => <div className="skeleton skeleton-card" key={index}/>)}</div></div>; }
export function ErrorState({ message, retry }: { message: string; retry: () => void }) { return <div className="state error" role="alert"><span className="state-symbol">!</span><h2>Không thể tải dữ liệu</h2><p>{message}</p><button type="button" onClick={retry}>Thử lại</button></div>; }
export function EmptyState({ message, title = 'Chưa có dữ liệu' }: { message: string; title?: string }) { return <div className="state" role="status"><span className="state-symbol">—</span><h2>{title}</h2><p>{message}</p></div>; }
